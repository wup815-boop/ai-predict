/**
 * 完全自動パイプライン構成図
 * 
 * 人間の操作が必要なのは「A/Bを選ぶ」だけ。
 * それ以外はすべて自動で回る。
 * 
 * ┌──────────────────────────────────────────────────────┐
 * │  GitHub Actions (毎日 UTC 06:00 = JST 15:00)         │
 * │                                                      │
 * │  Step 1: fetch-products.ts                           │
 * │    PH GraphQL → AIフィルタ → GitHub確認 → DB保存      │
 * │                                                      │
 * │  Step 2: create-pairs.ts                             │
 * │    未ペア製品 → ランダムマッチ → AI予測 → DB保存       │
 * │                                                      │
 * │  Step 3: judge-pairs.ts                              │
 * │    30日経過ペア → Stars取得 → 増加率判定 → 結果更新    │
 * └──────────────────────────────────────────────────────┘
 *            │ データはSupabaseに自動保存
 *            ▼
 * ┌──────────────────────────────────────────────────────┐
 * │  Vercel (Next.js) — 常時公開                         │
 * │                                                      │
 * │  /              → 今日のペアを自動表示                 │
 * │  /results       → 判定済みペアを自動表示              │
 * │  /history       → ログイン中ユーザーの履歴            │
 * │                                                      │
 * │  DBにペアがあれば自動で画面に出る。                    │
 * │  人間がやることは「AかBを押す」だけ。                  │
 * └──────────────────────────────────────────────────────┘
 * 
 * 人間の定常作業: なし
 * 人間の初期作業: アカウント作成 + デプロイ（1回だけ）
 */
